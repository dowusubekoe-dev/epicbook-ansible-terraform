- name: Install Nginx
  apt:
    name: nginx
    state: present
    update_cache: yes

- name: Configure reverse proxy
  template:
    src: epicbook.conf.j2
    dest: /etc/nginx/sites-available/default
  notify:
    - restart nginx

- name: Enable Nginx
  service:
    name: nginx
    state: started
    enabled: yes